[package]
edition = "2018"
name = "napi_rs_xz"
version = "0.0.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
lzma-rs = {version = "0.2", features = ["stream"]}
napi = "1"
napi-derive = "2"

[build-dependencies]
napi-build = "1"

[profile.release]
lto = true
